export const storageKeys = {
  REFRESH_TOKEN: "refresh_token",
  ACCESS_TOKEN: "access_token",
};

export const EMAIL_REG_EXP = /^[A-Z0-9._%+-]+@[A-Z0-9.-]+.[A-Z]{2,4}$/i;
export const Password_REG_EXP = /(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(.{8,15})/;
